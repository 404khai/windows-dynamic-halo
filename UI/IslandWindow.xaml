<Window x:Class="WindowsDynamicHalo.UI.IslandWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WindowsDynamicHalo.ViewModels"
        xmlns:uc="clr-namespace:WindowsDynamicHalo.UI.Controls"
        mc:Ignorable="d"
        Title="Dynamic Island"
        Height="35" 
        Width="70"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeChanged="OnSizeChanged">
    
    <Window.DataContext>
        <vm:IslandViewModel />
    </Window.DataContext>

    <Border x:Name="RootBorder" Background="Black" 
            CornerRadius="17"
            HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch"
            RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <ScaleTransform ScaleX="1" ScaleY="1"/>
        </Border.RenderTransform>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Background Click Target (Toggle Expand) -->
            <Button Grid.Row="0" Grid.RowSpan="2"
                    Command="{Binding ToggleExpandCommand}"
                    Background="Transparent" BorderThickness="0"
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                    Opacity="0"/>

            <!-- Media State (Compact View) -->
            <Grid Grid.Row="0" Visibility="{Binding HasMedia, Converter={StaticResource BoolToVis}}" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Album Art with fade/scale on change -->
                <Grid Margin="12,0,0,0" VerticalAlignment="Center">
                    <Border Width="36" Height="36" CornerRadius="6" Background="#222">
                        <Border.Effect>
                            <DropShadowEffect ShadowDepth="0" BlurRadius="10" Color="#55000000"/>
                        </Border.Effect>
                        <Image x:Name="AlbumArt"
                               Source="{Binding Media.AlbumArt}"
                               Stretch="UniformToFill"
                               ClipToBounds="True"/>
                    </Border>
                    <Grid.Triggers>
                        <EventTrigger RoutedEvent="Binding.TargetUpdated">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="AlbumArt"
                                                     Storyboard.TargetProperty="Opacity"
                                                     From="0" To="1"
                                                     Duration="0:0:0.20">
                                        <DoubleAnimation.EasingFunction>
                                            <QuadraticEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                     Storyboard.TargetName="AlbumArt"
                                                     From="0.94" To="1.0"
                                                     Duration="0:0:0.20">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                     Storyboard.TargetName="AlbumArt"
                                                     From="0.94" To="1.0"
                                                     Duration="0:0:0.20">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Grid.Triggers>
                    <Grid.RenderTransform>
                        <ScaleTransform/>
                    </Grid.RenderTransform>
                </Grid>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0" IsHitTestVisible="False"
                            Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="{Binding Title}" Foreground="White" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding Artist}" Foreground="#AAAAAA" FontSize="11" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                
                <!-- Controls + Visualizer -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,15,0" VerticalAlignment="Center">
                    <uc:MediaControls Margin="0,0,8,0" Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVis}}"/>
                    <uc:Visualizer IsActive="{Binding Media.IsPlaying}"/>
                </StackPanel>
            </Grid>
            
            <!-- Expanded State: Seek Slider -->
            <Grid Grid.Row="1" Margin="20,0,20,15" Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVis}}">
                 <Grid.ColumnDefinitions>
                     <ColumnDefinition Width="Auto"/>
                     <ColumnDefinition Width="*"/>
                     <ColumnDefinition Width="Auto"/>
                 </Grid.ColumnDefinitions>
                 
                 <TextBlock Text="{Binding Media.Position, StringFormat={}{0:mm\\:ss}}" Foreground="#888" FontSize="10" VerticalAlignment="Center" Margin="0,0,8,0"/>
                 
                 <Slider Grid.Column="1"
                         Minimum="0" 
                         Maximum="{Binding Media.TotalDurationSeconds, Mode=OneWay}" 
                         Value="{Binding Media.CurrentPositionSeconds, Mode=TwoWay}"
                         Thumb.DragStarted="OnSliderDragStarted"
                         Thumb.DragCompleted="OnSliderDragCompleted"
                         VerticalAlignment="Center"
                         IsMoveToPointEnabled="True"
                         Margin="0,0,0,0"/>

                 <TextBlock Grid.Column="2" Text="{Binding Media.Duration, StringFormat={}{0:mm\\:ss}}" Foreground="#888" FontSize="10" VerticalAlignment="Center" Margin="8,0,0,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>
