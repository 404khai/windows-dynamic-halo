<Window x:Class="WindowsDynamicHalo.UI.IslandWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WindowsDynamicHalo.ViewModels"
        mc:Ignorable="d"
        Title="Dynamic Island"
        Height="{Binding Height}" 
        Width="{Binding Width}"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeChanged="OnSizeChanged">
    
    <Window.DataContext>
        <vm:IslandViewModel />
    </Window.DataContext>

    <Border Background="Black" 
            CornerRadius="17"
            HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch">
        <Grid>
            <!-- Media State -->
            <Grid Visibility="{Binding HasMedia, Converter={StaticResource BoolToVis}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Placeholder Art -->
                <Image Source="/Assets/placeholder.png" 
                       Width="30" Height="30" 
                       Margin="15,0,0,0"
                       VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                    <TextBlock Text="{Binding Title}" Foreground="White" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding Artist}" Foreground="#AAAAAA" FontSize="11" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                
                <!-- Animated Visualizer (Fake) -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,15,0" VerticalAlignment="Center">
                    <Border Width="3" Height="10" Background="#33FF33" CornerRadius="1" Margin="1"/>
                    <Border Width="3" Height="18" Background="#33FF33" CornerRadius="1" Margin="1"/>
                    <Border Width="3" Height="8" Background="#33FF33" CornerRadius="1" Margin="1"/>
                </StackPanel>
            </Grid>
            
            <!-- Idle State (Implicitly visible if Media State is Collapsed, but since we just have a black border, it looks like a dot/pill) -->
        </Grid>
    </Border>
</Window>
