<UserControl x:Class="WindowsDynamicHalo.UI.Controls.MediaControls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Height="28">
        <StackPanel Orientation="Horizontal">
            <!-- Prev -->
            <Button Command="{Binding Media.SkipPreviousCommand}"
                    Width="24" Height="24"
                    Background="Transparent" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Path Data="M6,6h2v12H6V6z M9.5,12l8.5,6V6L9.5,12z" Fill="White" Stretch="Uniform" Margin="4"/>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <!-- Play/Pause -->
            <Button Command="{Binding Media.PlayPauseCommand}"
                    Width="28" Height="28"
                    Margin="10,0"
                    Background="Transparent" 
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="{TemplateBinding Background}">
                            <!-- <Ellipse Fill="#333" /> --> <!-- Removed circle bg to match design -->
                            <Path x:Name="Icon" 
                                  Fill="White" 
                                  HorizontalAlignment="Center" 
                                  VerticalAlignment="Center"
                                  Data="M8,5.14V19.14L19,12.14L8,5.14Z"/> <!-- Play Icon -->
                        </Grid>
                        <ControlTemplate.Triggers>
                            <DataTrigger Binding="{Binding Media.IsPlaying}" Value="True">
                                <Setter TargetName="Icon" Property="Data" Value="M6,19h4V5H6V19z M14,5v14h4V5H14z"/> <!-- Pause Icon -->
                            </DataTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <!-- Next -->
            <Button Command="{Binding Media.SkipNextCommand}"
                    Width="24" Height="24"
                    Background="Transparent" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                         <Path Data="M6,18l8.5-6L6,6V18z M16,6v12h2V6H16z" Fill="White" Stretch="Uniform" Margin="4"/>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
